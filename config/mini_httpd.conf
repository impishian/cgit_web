# 监听端口与绑定 IP
port=18080
host=127.0.0.1

# 网页根目录
dir=__ROOT_DIR__

# 运行用户（由 start.sh 注入当前用户名）
user=__RUN_USER__

# 关键：必须显式忽略 chroot，否则 cgit 在隔离环境下找不到系统依赖
nochroot

# CGI 匹配规则：用 ** 让根目录和子目录下的 .cgi 都能命中
cgipat=**.cgi

# 错误日志路径
logfile=/tmp/mini_httpd.log

# 进程号文件，便于脚本停止服务
pidfile=/tmp/mini_httpd.pid
